# MockCharles 领域术语

本词汇表统一桌面代理、Mock 配置与业务复现的命名。

## Language

**设备来源（DeviceSource）**：
手机请求到达代理时的实际来源 IP，是本工具区分设备配置的依据，不是永久硬件身份。
_Avoid_：设备指纹、客户端账号

**独立规则集（RuleSet）**：
供设备在独立 Mock 模式下选择的有序规则集合，可被多台设备复用。
_Avoid_：Mock 组、运行组

**Mock 规则（MockRule）**：
具有稳定身份的接口匹配与响应配置对象；独立模式按规则集顺序选择，组步骤可引用其响应。
_Avoid_：接口数据、执行步骤

**响应版本（ResponseRevision）**：
一次保存形成的固定响应或随机响应配置，以及其可定位的版本身份。
_Avoid_：当前响应结果、实时业务记录

**业务记录（BusinessRecord）**：
用户在内部数据集中维护、供编辑响应时选用的业务 JSON 记录。
_Avoid_：运行上下文、在线查询结果

**响应样本（ResponseSample）**：
从捕获流量等来源提取并独立保存、供编辑 Mock 使用的响应内容。
_Avoid_：流量记录、随机模板

**列表项模板（ItemTemplate）**：
编辑时根据一条业务记录构造的条目结构，随机列表中的新条目以它为基础，未配置随机规则的字段保留模板值。
_Avoid_：抽样记录池、跨接口变量

**Mock 组（MockGroup）**：
有序接口步骤的业务复现定义；运行时仅当前步骤可返回其 Mock 响应。
_Avoid_：独立规则集、批量请求任务

**组运行（ScenarioRun）**：
某设备来源对某个组顺序快照的一次执行，拥有独立当前步骤和结束状态。
_Avoid_：MCP 会话、设备连接

**当前步骤（CurrentStep）**：
运行中唯一可消费的有效组步骤；完整响应成功写入连接后才进入下一有效步骤。
_Avoid_：最近请求、当前编辑行

**通用 Header 规则（GeneralHeaderRule）**：
在可处理 HTTP 请求/响应链路上普遍适用的修改定义，先于命中接口自身的 Header 操作执行。
_Avoid_：Mock 专属 Header、原始 Header

**流量记录（FlowRecord）**：
一次实际代理请求及响应的观察记录，可关联实际使用的 Mock、组运行和版本。
_Avoid_：业务记录、响应模板
