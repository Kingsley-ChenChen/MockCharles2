# 设计完整性核对

更新：2026-09-11。已完成总体方案、模块设计与 UI 评审，HTTP/固定规则开发验收版已实现并通过真实端到端验证。完整范围仍在推进，最新状态见第 10 节；以下早期检查点保留时间上下文。

## 本轮规划调整（2026-09-10）

已按用户要求将产品入口整理为设备管理、规则管理、规则集管理、流量，并明确全部 IP/单 IP × 流式/目录式。见 [四模块项目规划](project-plan.md)。旧多入口导航和重复布局已替换；顺序组、数据材料等已有能力归并到四模块，未取消。D17—D21 记录新增边界建议；本轮只调整规划文档，不代表应用实现或产品细则全部确认。

## 1. 交付范围

[详细设计索引](design/README.md) 包含 10 份主题文档：开发环境、模块接口、代理与 HTTPS、匹配与 Header、数据库版本、随机生成、组运行、桌面交互、管理/MCP 接口、验证与实施顺序。

配套 [领域术语](../CONTEXT.md) 和 [决策登记](design/decisions.md) 统一命名及未决行为。原 [功能设计](design-draft.md)、[架构总览](architecture.md) 保留为总体说明；技术事实依据见 [桌面选型](research/desktop-stack.md) 和 [AI 接入研究](research/ai-integration.md)。

## 2. 已确认且已同步

- Windows/macOS 桌面，Wails v2 + Go + Vue3/TypeScript + SQLite；内部前后端分层，共同打包。
- 同代理端口、来源 IP 区分设备；每设备独立选择三种模式与组运行。
- 每设备可选择一个独立规则集，规则集可被多设备复用；按列表顺序取首个启用命中规则。
- 随机列表采用单记录模板生成新条目，仅随机配置字段，其他字段保持模板值。
- 完整响应成功写入连接后才推进当前步骤；生成/写出失败均不推进。
- 组未命中及完成后真实转发，通用 Header 仍生效；不回退独立 Mock。
- 响应配置下一请求生效，在途保留版本；组顺序与步骤开关下次开始/重置生效。
- 切出组模式或改绑另一个组停止旧运行，手动开始；退出停止代理、重启不自动续跑。
- 2026-09-10 补充确认 D07：应用启动后不自动监听，需手动开启代理；设备选择恢复按已确认 D07 执行。
- 2026-09-10 补充确认 D11：首版支持 Windows x64（Intel/AMD）、macOS Intel 和 Apple Silicon；Windows ARM64 不纳入首版。最低系统版本和分发细则仍待确认。
- AI 数据与规则 CRUD 保留，首版禁止切模式、绑定及运行开始/停止/重置。

## 3. 仍需确认的范围

D15 已于 2026-09-10 确认 B 方案：多个项目可同时服务，每个来源 IP 同时只绑定一个项目；查看/编辑项目与设备实际绑定独立。项目是测试配置空间，不要求与代码仓库或 App 一一对应。跨项目改绑的在途处理与运行清理需结合 D05 细化。

[决策登记 D04—D21](design/decisions.md) 集中记录：并发候选等待、控制操作与在途请求竞争、Header/匹配/排序编辑生效、设备选择恢复、随机切换细则、捕获保留、引用删除及部分 AI 配置权限、目标平台分发矩阵、性能规模、补充功能、匹配操作符、项目作用域、空组与草稿。D07 已确认保留 IP、备注、项目、模式和规则集选择，不自动监听或续跑。

D11 的 CPU 架构范围已确认，最低系统版本及分发细则仍待确认。这些未决产品行为已有可审阅建议，但没有将其默认采纳。相关模块实施前需要确认，不能说全部规格已经无歧义。

## 4. 环境与验证状态

已读取本机 Node v22.22.0、npm 10.9.4、Git 2.52.0.windows.1。Go/Wails 不在当前 PATH，不等于确认未安装。没有安装依赖、创建业务数据库或修改系统/手机代理及证书。

设计文档进行文件完整性、链接及确认项一致性检查；这不是应用测试。代理库、数据库驱动和 MCP 客户端兼容仍需真实联调，包体/内存/延迟需要实测。

## 5. 当前推进计划

用户本轮明确功能分析 → 模块详细设计 → UI 设计 → 界面实现 → 测试用例编写，取代之前直接推进 UI 的次序。已整理 [五阶段计划](superpowers/plans/2026-09-10-project-delivery.md)，将旧设计材料作为各阶段输入，保留既有产品确认。

| 阶段 | 当前状态 | 下一产出 |
| --- | --- | --- |
| 功能分析 | 首轮逐项分析已完成，行为收口中 | [功能规格](functional-analysis.md)；D20/D07 与 D09 保留策略已确认，继续细化剩余行为 |
| 模块详细设计 | 四模块契约草案已具备，剩余行为待收口 | [设备](design/modules/devices.md)、[规则](design/modules/rules.md)、[规则集](design/modules/rule-sets.md)、[流量](design/modules/traffic.md) |
| UI 设计 | UI 评审稿 01 已生成，等待用户评审 | [交互设计稿](design/ui/review.html)、[UI 规格](design/ui-spec.md)；四模块与流量四组合可预览 |
| 界面实现 | 未开始 | 可运行界面、演示/真实连接状态、必要验证记录 |
| 测试用例编写 | 已有验证场景清单 | 正式用例、覆盖矩阵、选定自动化脚本 |

本轮完成的是阶段规划，不是上述五阶段的全部产物。当前下一步先完成功能分析；实现开始前收口相应设计。完整测试用例集中在第五阶段，前面先明确验收条件，开发中仍执行必要验证。真实代理、证书、持久化和运行状态需要核心支持，演示页面不计作真实功能联调通过。

## 6. 自主推进与协作检查点

用户已授权逐步自主实现完整软件，在关键产品决定、UI 设计稿和需要人工测试时协作。功能分析已形成稳定编号、输入输出、边界、验收与跨模块流程，详见 [功能分析与检查点](functional-analysis.md)。D20 已确认按项目统一改写 Header，未绑定项目不改写，接口专用最后覆盖。阶段一尚有行为待收口，不标为全部完成；继续处理目录/编排交互及容量边界。

真实核心实现、用例执行、缺陷修复和打包属于完整软件目标；仅界面演示或仅用例编写不能宣称软件完成。

设备字段、命令、事件和重启行为已形成详细设计草案，属于等待决策期间对稳定需求的细化；不代表跳过功能分析评审直接实现。D07 已确认保留配置，不自动监听/续跑。

D13 部分已确认：只在目录式提供流量导出，首版不做流量导入；D09 已确认关闭重启不恢复上次流量，只有主动导出留存。

## 7. 当前检查点（2026-09-11）

D05 已确认切模式/改绑/重置允许已有请求按旧计划完成，旧回调不推进新运行；退出排空时限尚未确定。规则与规则集模块已补齐对象、命令、版本、引用、UI 状态和验收路径，四模块详细设计草案已齐。D06（保存生效时机）已确认并同步；未决行为未标为确认。尚未应用编码或执行软件测试。

## 8. UI 评审检查点（2026-09-11）

D06 已确认普通规则匹配/Header/启用/排序下一请求生效；顺序匹配/编排下次开始或重置生效；响应仍下一请求生效，在途保留完整版本。基于已确认主流程制作了可点击 UI 稿，剩余产品细则不因原型生成自动确认。

使用本机 Edge 无头浏览器检查：流量四组合、IP 完整显示、搜索空状态、查看项目与流量筛选隔离、设备演示保存/跳转、JSON 校验/预览、顺序调整与重置、密度切换、1100px 窗口布局、从流量创建草稿及项目隔离。无页面脚本错误；截图已人工查看并修正 IP 列与重置显示。仅属原型验证，真实代理/证书/存储和正式测试未实现。

设计预览通过本机 127.0.0.1:4173 提供，文件见 UI 规格；用户下一步评审布局与密度，再按确认结果细化并进入正式实现。Go/Wails 当前仍未在 PATH 检出，工程准备时再处理。

## 9. 设备与规则集 UI 反馈（2026-09-11）

设备页原布局已获用户认可；按 D22 将所有运行控制移到设备侧，规则集页只编辑编排并查看关联设备。D23 已确认多对多关联、每设备同时只启用一个集合。设备页增加关联管理、候选选择、实际启用状态和设备侧启用/停止/重置；两台同项目设备复用同集合，运行状态按 IP 独立。

UI 评审稿 02 已通过原型检查：规则集无运行按钮；候选选择不自动启用；两设备启用/停止/重置独立；关联保存不启动；活动关联移除保护；未绑定来源不能启动；新增演示设备跳流量正确为空；目录导出保留；1100px 无横向窗口溢出。浏览器无脚本错误。此为演示交互验证，非真实核心测试。UI 图已更新。

## 10. 正式实现检查点（2026-09-11）

用户接受 UI 02 并要求继续后，进入正式工程。开发分支 `codex/desktop-foundation`，采用 Go/Wails/Vue/SQLite。已完成 HTTP 与固定优先规则的可运行纵向链路：设备发现及持久配置、同项目多关联/单启用、规则与编排保存、项目 Header、实际转发/Mock、会话流量四组合。停止规则保留上次启用选择。

Go 工具链已通过官方校验并安装于项目 `.tools`，Wails doctor 确认本机 WebView2 可用。前端构建、Go 测试与 vet、浏览器界面回归、Windows x64 编译已完成；另使用真实 Wails 开发桥接和本机 HTTP 上游，通过界面新建项目/规则/规则集/设备配置，验证转发 → Mock → 停用后转发及实际流量显示。

当前实际实现范围与历史草案严格分开：HTTPS/CA、顺序运行、随机响应、样本、导出、MCP、删除策略尚未实现；目录导出显示待实现且流式无导出。500 条/64 KiB 捕获、30 秒请求/3 秒停止等待为开发版临时上限，不视为用户批准了最终容量/退出策略。竞态检测、macOS 与手机实机未完成。

当前使用入口见根目录 [README](../README.md)，下一人工检查点见 [开发验收清单](testing/desktop-foundation.md)。用户只需反馈实机结果；下一阶段按原总体计划继续 HTTPS 与顺序运行，关键未决项在对应阶段询问。

## 11. HTTPS 与证书检查点（2026-09-11）

用户要求继续后，确认 D24“指定域名解密”。已实现加密隧道、名单内 TLS 解密、复用原设备/Header/固定规则/流量链路，以及设备页 CA 生成、指纹与有效期、公共证书本机保存和手机下载。CA 不自动替换；Windows 使用 DPAPI 保护持久材料，其他平台限制文件权限。

25 项 Go 测试（17 核心、8 证书）、4 项前端状态测试及 UI 回归覆盖本阶段。真实 Wails/Node TLS 验收已验证公共 DER 与指纹、受信任客户端连接、默认 443 HTTPS Mock、项目 Header 和实际流量展示。独立审查修正默认端口/IPv6/CA 缺失时关闭解密及隧道半关闭与即时记录问题。

本阶段尚需手机实机安装与信任、真实自研 App 的网络栈兼容性验收；不承诺绕过证书固定、HTTP/3/QUIC。下一人工检查点见 [HTTPS 验收清单](testing/https-certificates.md)。顺序运行、随机响应、样本、导出等后续范围不删除。前述第 10 节“HTTPS 尚未实现”是上一检查点的历史状态，以本节为准。

## 12. HTTPS 入口迁移（2026-09-12）

按 D25，设备页已移除 HTTPS 解密开关、域名名单和保存控件，仅保留本机证书与安装弹窗。流量模块后续实现启停、名单管理以及每条请求右键“当前域名解密”（含 CONNECT 隧道记录）。本次不实现流量入口，后端 HTTPS 能力及已保存 TLS 配置继续保留。当前新配置的人工解密验收需等待流量入口；证书接入与隧道转发可以继续验收。
